# -*- coding: utf-8 -*-
"""
Общая requests.Session для всех обращений к llama-server.

Раньше каждый вызов LLM (основной цикл, автономия, извлечение фактов,
рефлексия, сон-консолидация) делал отдельный requests.post(), который под
капотом открывает НОВОЕ TCP-соединение на каждый запрос. На loopback это
дёшево, но не бесплатно, а таких точек вызова теперь пять — используя общую
Session с пулом соединений (keep-alive), повторные запросы к тому же
LLM_API_URL переиспользуют уже установленное соединение вместо повторного
handshake на каждый раз.
"""
import requests

SESSION = requests.Session()
